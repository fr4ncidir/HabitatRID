USAGE:

ridScript.sh setta il sistema per il funzionamento di ridReader e/o avvia la simulazione del reader.

$ ridScript.sh help 
		mostra il file di aiuto
		
$ ridScript.sh setup
		installa i pacchetti essenziali (lgsl e lcurl, RPi.GPIO)
		
$ ridScript.sh simulate <file di setup>
		simula il RID con le imposizioni definite nel file di configurazione
		
$ ridScript.sh compile <indirizzo della porta usb>
		compila i file .c in ridReader
		
$ ridScript.sh run
		mostra le istruzioni per eseguire il run di ridReader

Per informazioni su ridReader, fai
$ ./ridReader help

Per informazioni su ridSimulation.py, fai
$ python ridSimulation.py help

SIMULATION FILE INPUT FORMAT (JSON):
{
	"sepa_ip":"192.168.1.52",
	"sepa_update_port":8000,
	"simulation":"file", 		{"random"}
	"type":"button", 			{"timer"}
	"timing":10,	 			{(ms) ignored if type is "button"}
	"iterations":7,
	"x_topleft":15,				{we assume x_btmright=0}
	"y_topleft":10,				{we assume y_btmright=0}
	"ridUid":"137",				{it's a string!}
	"locations": [				{ignored if simulation if "random"}
		{
			"x":3.5,
			"y":1.9
		},
		{
			"x":6.2,
			"y":7.1
		}
	]
}

Autore: Francesco Antoniazzi <francesco.antoniazzi@unibo.it>
